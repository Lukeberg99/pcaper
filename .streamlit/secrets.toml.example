# Copy this file to .streamlit/secrets.toml for local development,
# or paste these keys into Streamlit Cloud (App → Settings → Secrets).
# Never commit the real secrets.toml.

OHC_API_KEY = "sk_REPLACE_WITH_YOUR_KEY"

# Optional override of the API base endpoint:
# OHC_API_URL = "https://api.onlinehashcrack.com/v2"
